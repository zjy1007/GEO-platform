# --- web / core (needed from P0) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0
alembic>=1.13
asyncpg>=0.29
pyyaml>=6.0
httpx>=0.27

# --- queue (Arq chosen over Celery for a small team) ---
arq>=0.26
redis>=5.0

# --- model calls / resilience (P1) ---
tenacity>=8.3

# --- file parsing / uploads (P1) ---
python-multipart>=0.0.9
pypdf>=4.2
python-docx>=1.1
openpyxl>=3.1

# --- evidence crawl + vector (P3) ---
beautifulsoup4>=4.12
trafilatura>=1.10
pgvector>=0.3

# --- account pool / web channel (P2) ---
playwright>=1.44

# --- observability ---
prometheus-client>=0.20
sentry-sdk>=2.5

# --- dev / test ---
pytest>=8.2
pytest-asyncio>=0.23
